# Results

- `interim/` contains current working exports used while drafting the manuscript.
- `generated/` is intended for final outputs recreated by the executable scripts.

Before publication, regenerate final result CSV files, statistical tests, SHAP outputs, and plots from the final online-compatible pipeline.
